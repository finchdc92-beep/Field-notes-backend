{
  "name": "field-notes-backend",
  "version": "1.0.0",
  "description": "Proxy server that holds the Anthropic API key server-side for the Field Notes app.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
